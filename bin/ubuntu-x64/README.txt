To use these binaries, run sth like:

$ LD_LIBRARY_PATH=$(pwd):$LD_LIBRARY_PATH fastvideofeat -i video.avi # and add other arguments as you wish

or modify LD_LIBRARY_PATH permanently as done in e.g. samples/compute_mpeg_features.sh
